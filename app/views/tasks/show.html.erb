
<h1><%= @task.id%> <%=@task.title%>  </h1>
<h3><%=@task.details%></h3>
<%= link_to "Edit #{@task.title}" , edit_path(@task) %>
<%= link_to "Delete",
            task_path(@task),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
